# finance-tracker-using-chart.js
Project 1: Personal Finance Tracker HTML forms for data entry (because manually typing into console.log gets old) Local storage for persistence DOM manipulation for displaying data Basic charts using Chart.js (make those numbers look fancy) CSV export functionality (for the spreadsheet enthusiasts)
